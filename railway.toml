# Let the Dockerfile CMD (scripts/start.sh) own the process.
# Do not override startCommand here — that was preventing a clean boot.
[deploy]
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/"
healthcheckTimeout = 300
