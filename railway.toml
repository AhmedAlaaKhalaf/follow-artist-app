# Railway: listen on 0.0.0.0 and the injected PORT (do not assume 3000).
[deploy]
startCommand = "HOST=0.0.0.0 npx prisma migrate deploy && HOST=0.0.0.0 npx react-router-serve ./build/server/index.js"
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/"
healthcheckTimeout = 300
